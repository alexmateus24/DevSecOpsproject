name: AWS Automation Project 1

on:
  push:
    branches:
      - main

jobs:
  deploy-and-scan:
    name: Deploy WordPress on EC2 and IaC Scans
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Terraform setup
      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.6   # use a modern stable version

      # AWS credentials setup
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-west-2

      # Initialize Terraform
      - name: Terraform Init
        run: terraform init
        working-directory: ./DevSecOpsproject

      # Terraform Apply
      - name: Terraform Apply
        run: terraform apply -auto-approve
        working-directory: ./DevSecOpsproject

      # Get WordPress instance public IP
      - name: Get WordPress Instance IP
        id: get_ip
        run: echo "ip_address=$(terraform output -json instance_public_ip | jq -r '.')" >> $GITHUB_OUTPUT
        working-directory: ./DevSecOpsproject
